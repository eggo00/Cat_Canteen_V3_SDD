{
  "$schema": "https://json.schemastore.org/zeabur",
  "build": {
    "type": "dockerfile",
    "dockerfile": "Dockerfile"
  },
  "start": {
    "command": "uvicorn src.api.main:app --host 0.0.0.0 --port 8000"
  },
  "healthcheck": {
    "path": "/health",
    "port": 8000
  },
  "expose": {
    "port": 8000
  }
}
