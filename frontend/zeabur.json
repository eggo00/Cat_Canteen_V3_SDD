{
  "$schema": "https://json.schemastore.org/zeabur",
  "build": {
    "type": "dockerfile",
    "dockerfile": "Dockerfile",
    "build_args": {
      "VITE_API_BASE_URL": "${VITE_API_BASE_URL}"
    }
  },
  "expose": {
    "port": 8080
  }
}
